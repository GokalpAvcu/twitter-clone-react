import React, { useEffect } from 'react';

const Content = () => {
    useEffect(() => {});
    return (
        <main className="flex-1 bg-purple-200">
            <h1>Content</h1>
        </main>
    );
};

export default Content;
